name: sensio_ring
description: Production-ready Flutter BLE app for SENSIO Ring wearable device.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.32.0
  fl_chart: ^0.66.0
  equatable: ^2.0.5
  uuid: ^4.2.2
  permission_handler: ^11.3.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
